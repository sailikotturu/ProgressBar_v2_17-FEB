<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.sailik.progressbar_17_feb.MainActivity">

    <Button
        android:text="Start"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:layout_centerVertical="true"
        android:layout_marginLeft="@dimen/startbtn_marginleft"
        android:id="@+id/button"
        android:clickable="true"/>

    <ProgressBar
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="@dimen/progressbar_height"
        android:layout_alignParentTop="true"
        android:layout_marginTop="@dimen/progressbar_margintop"
        android:id="@+id/progressBar"
        android:indeterminate="false"
        android:progressTint="@color/progress_tint"
        android:progressBackgroundTint="@color/progressback_tint"/>

    <Button
        android:text="Next Screen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/button"
        android:layout_toRightOf="@+id/button"
        android:layout_toEndOf="@+id/button"
        android:layout_marginLeft="@dimen/nextscrbtn_marginleft"
        android:layout_marginStart="@dimen/nextscrbtn_marginstart"
        android:id="@+id/button_next" />
    <!--android:background="#1a621a"-->
        <!--android:progressTint="#ff0600"-->
        <!--android:progressBackgroundTint="#4d34ff"/>-->
        <!--android:progressDrawable="@drawable/custom_progress"/>-->
        <!--android:background="#000"/>-->
</RelativeLayout>
